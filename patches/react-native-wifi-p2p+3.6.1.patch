diff --git a/node_modules/react-native-wifi-p2p/android/src/main/java/io/wifi/p2p/WiFiP2PManagerModule.java b/node_modules/react-native-wifi-p2p/android/src/main/java/io/wifi/p2p/WiFiP2PManagerModule.java
index f296f79..4965649 100644
--- a/node_modules/react-native-wifi-p2p/android/src/main/java/io/wifi/p2p/WiFiP2PManagerModule.java
+++ b/node_modules/react-native-wifi-p2p/android/src/main/java/io/wifi/p2p/WiFiP2PManagerModule.java
@@ -89,8 +89,8 @@ public class WiFiP2PManagerModule extends ReactContextBaseJavaModule
 
   @ReactMethod
   public void init(Promise promise) {
-    if (manager != null) { // prevent reinitialization
-      promise.reject("0x2", this.getName() + " module should only be initialized once.");
+    if (manager != null && channel != null) { // initialization is idempotent across JS restarts
+      promise.resolve(true);
       return;
     }
 
@@ -119,8 +119,10 @@ public class WiFiP2PManagerModule extends ReactContextBaseJavaModule
         activity.registerReceiver(receiver, intentFilter);
 
         promise.resolve(manager != null && channel != null);
+        return;
       } catch (NullPointerException e) {
         promise.reject("0x1", "can not get WIFI_P2P_SERVICE");
+        return;
       }
     }
 
