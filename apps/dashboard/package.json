{
  "name": "crowdflow-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Operator console. Dense, complete, read from three metres.",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun tsc --noEmit && bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "typecheck": "bunx --bun tsc --noEmit",
    "test": "bunx --bun vitest run"
  },
  "dependencies": {
    "@crowdflow/api": "0.2.0",
    "@crowdflow/contracts": "0.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  }
}
