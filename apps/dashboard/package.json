{
  "name": "crowdflow-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Operator console. Dense, complete, read from three metres.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "biome lint src vite.config.ts index.html --diagnostic-level=error",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@crowdflow/api": "0.2.0",
    "@crowdflow/contracts": "0.2.0",
    "@crowdflow/core": "0.2.0"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
