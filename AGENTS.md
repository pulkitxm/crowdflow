# Expo SDK 57

Read the exact versioned documentation at https://docs.expo.dev/versions/v57.0.0/ before changing Expo APIs.

This app requires an Expo development build because Bluetooth advertising, UDP/mDNS, and Wi-Fi Direct use native modules. Expo Go is not a supported radio runtime.
